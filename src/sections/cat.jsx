import heroExtra from '../assets/hero-video.webp';

export default function Cat() {
  return (
    <section id="cat" className="section">
      <div className="media">
        <img src={heroExtra} alt="Michi el gato" className="mediaImg" />
      </div>
    </section>
  );
}   